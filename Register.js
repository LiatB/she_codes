/* form validation*/
/* check empty */

 /*$.get( "myLogin.php", function( data ) {
    alert( data );
	}); */
	/*
	$(".form-horizontal").validator()
	
	function validate()
{
 
   if( document.registrationForm.firstname.value == "" )
   {
     alert( "Please provide your name!" );
     document.registrationForm.name.focus() ;
     return false;
   }
   if( document.registrationForm.lastname.value == "" )
   {
     alert( "Please provide your last name!" );
     document.registrationForm.lastname.focus() ;
     return false;
   }
 
   if(  document.registrationForm.email.value == "" )
   {
     alert( "Please provide your Email!" );
     document.registrationForm.email.focus() ;
     return false;
   }
     
   if(document.registrationForm.pwd.value  =="")
   {
   alert("please provide a password")
   document.registrationForm.pwd.focus();
   return false;
   }
   if(document.registrationForm.cnfrm_pwd.value  =="")
   {
   alert("please confirm your password")
   document.registrationForm.pwd.focus();
   return false;
   }
    if(document.registrationForm.pwd.value !== document.registrationForm.cnfrm_pwd.value)
   {
   alert("password doesn't match")
   document.registrationForm.cnfrm_pwd.focus();
   return false;
   }
   return( true );
  */

   

